<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BookLibrary" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/app_theme</item>
        <item name="colorSurface">#FFFFFF</item>
        <item name="android:textColor">#000000</item>
    </style>

    <style name="Theme.BookLibrary" parent="Base.Theme.BookLibrary" />

    <style name="Theme.BookLibrary.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/app_theme</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/books_splashscreen</item>
        <item name="postSplashScreenTheme">@style/Theme.BookLibrary</item>
    </style>
</resources>